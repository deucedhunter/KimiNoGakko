@model InstructorDetailsVM

@{
    ViewData["Title"] = "Details";
}

<h2>Details</h2>

<div class="row">
    <div class="col-md-4">
        <h4>Instructor</h4>
        <hr />
        <dl class="dl-horizontal">
            <dt>
                @Html.DisplayNameFor(model => model.Instructor.FirstMidName)
            </dt>
            <dd>
                @Html.DisplayFor(model => model.Instructor.FirstMidName)
            </dd>
            <dt>
                @Html.DisplayNameFor(model => model.Instructor.LastName)
            </dt>
            <dd>
                @Html.DisplayFor(model => model.Instructor.LastName)
            </dd>
            <dt>
                @Html.DisplayNameFor(model => model.Instructor.BirthDate)
            </dt>
            <dd>
                @Html.DisplayFor(model => model.Instructor.BirthDate)
            </dd>
            <dt>
                @Html.DisplayNameFor(model => model.Instructor.Pesel)
            </dt>
            <dd>
                @Html.DisplayFor(model => model.Instructor.Pesel)
            </dd>
            <dt>
                @Html.DisplayNameFor(model => model.Instructor.HireDate)
            </dt>
            <dd>
                @Html.DisplayFor(model => model.Instructor.HireDate)
            </dd>
        </dl>
        <div>
            <a asp-action="Edit" asp-route-id="@Model.Instructor.ID">Edit</a> |
            <a asp-action="Index">Back to List</a>
        </div>
    </div>
    <div class="col-md-6">
        <table class="table table-striped">
            <thead>
                <tr>
                    <th>Nazwa przedmiotu</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model.Courses)
                {
                    <tr>
                        <td>
                            @item.Subject.Name
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>
